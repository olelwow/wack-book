import AppBar from "@mui/material/AppBar";

const LoginForm = () => {
  return <AppBar id="loginContainer"></AppBar>;
};

// Koden för knappar och fält läggs inne i AppBar elementet //

export default LoginForm;
